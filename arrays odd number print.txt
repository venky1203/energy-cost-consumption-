#include <stdio.h>
int main()
{
    int n,i,sum=0;
    scanf("%d",&n);
    int arr[i];
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
    
        for(i=0;i<=n;i++)
        {
            if(arr[i]%2==0)
            printf("%d",arr[i]);
            else
            sum=sum+arr[i];
        }
    
}